
Kind: TaskRun
apiVersion: 0.0.1
metadata:
  name: taskrun-egress-router-cni
spec:
  taskRef:  custom-openshiftbuild-task
  parameters:
    - name: repository-url
      value: 'https://github.com/openshift/egress-router-cni'
    - name: repository-name
      value: 'egress-router-cni'
    - name: branch
      value: 'master'
    - name: dockerfile
      value: 'Dockerfile.ocp'
